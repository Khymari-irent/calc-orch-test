# Workspace-local orchestration state

This directory holds optional automation preferences for one local workspace.
Only the example is committed. The active automation.toml is gitignored and is
created only after an exact AUTO APPROVE command followed by CONFIRM AUTOMATION.

Local state is a preference, not a reusable workflow approval. Never store
tokens, credentials, credential-bearing remote URLs, production data, or raw
command logs here.
